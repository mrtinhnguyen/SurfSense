import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";
import { BRAND_NAME } from "@/lib/brand";

export const baseOptions: BaseLayoutProps = {
	nav: {
		title: `${BRAND_NAME} Documentation`,
	},
};
